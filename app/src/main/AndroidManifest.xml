<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amilchov.digitalbag">

    <application
        android:allowBackup="true"
        android:icon="@drawable/digital_learning"
        android:label="@string/app_name"
        android:roundIcon="@drawable/digital_learning"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DocumentActivity"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AddLessonActivity" />
        <activity android:name=".LessonActivity" />
        <activity android:name=".AddCustomActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>